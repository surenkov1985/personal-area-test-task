import * as React from "react"
import { SubmitHandler, useForm } from "react-hook-form";
import { LoginContainer, Label, LabelText, InputEl, InputError, FormContainer, FalseCheck, Submit, CheckInput, CheckLabel, FalseCheckBack } from "./pages/loginPage/styled"

export const AddContact: React.FC = () => {

    return (
        <LoginContainer>
            ADD CONTACT
        </LoginContainer>
    )
}